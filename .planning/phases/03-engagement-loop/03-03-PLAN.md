---
phase: 03-engagement-loop
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - components/tree/SkillTreeFlow.tsx
  - components/tree/NodeDetailPanel.tsx
  - components/tree/HexagonNode.tsx
autonomous: true
requirements: [MAST-01, MAST-02, MAST-03, MAST-04]
---

<objective>
Ensure mastery progression is fully wired: node visuals update in real-time as mastery advances, expert/master particle effects display correctly, and the tree correctly reflects all 5 mastery levels.

Purpose: Visual feedback that mastery matters. Without real-time updates, completing challenges feels disconnected from the tree.
Output: Real-time mastery visual updates, verified particle effects, mastery level progression validation.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-engagement-loop/03-CONTEXT.md

<interfaces>
From Phase 2:
- HexagonNode.tsx has mastery-{level} CSS classes for all 6 states
- tree-styles.css has mastery-pulse and ember-rise animations
- SkillTreeFlow.tsx has prevLockMapRef for unlock detection

From Plan 03-02:
- refreshTree function in SkillTreeFlow
- ChallengeModal.onComplete triggers refresh
- API POST /api/challenges/{id}/submit returns newMastery with level-up info
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and enhance mastery visual updates and particle effects</name>
  <files>
    components/tree/HexagonNode.tsx,
    components/tree/tree-styles.css,
    components/tree/SkillTreeFlow.tsx
  </files>
  <read_first>
    components/tree/HexagonNode.tsx,
    components/tree/tree-styles.css,
    components/tree/SkillTreeFlow.tsx,
    lib/tree-utils.ts,
    components/tree/NodeDetailPanel.tsx
  </read_first>
  <action>
This task ensures the mastery progression visual loop is fully connected end-to-end:

**1. components/tree/HexagonNode.tsx** — Verify mastery class application:
- Confirm the `mastery-{level}` class is correctly applied to the wrapper div based on data.mastery
- Ensure expert/master nodes get the mastery-pulse class from tree-styles.css
- Add a `data-mastery` attribute to the wrapper div for debugging: `data-mastery={data.mastery}`
- If the mastery indicator icon path uses the mastery level, verify locked nodes show icon-node-locked.svg, not icon-node-undefined.svg

**2. components/tree/tree-styles.css** — Verify and enhance particle effects:
- Confirm @keyframes ember-rise exists and is applied to .mastery-expert::after and .mastery-master::after
- Ensure the ::after pseudo-element creates visible embers (not hidden behind the SVG)
- The ember should appear ABOVE the hexagon node: z-index or positioning fix if needed
- Add a second ember particle with different timing for master nodes (::before pseudo-element with offset timing)
- Verify mastery-pulse animation works for expert and master (3s infinite cycle)

**3. components/tree/SkillTreeFlow.tsx** — Ensure refresh updates mastery correctly:
- After refreshTree re-fetches data, verify that:
  - The prevLockMapRef comparison works (detects locked → unlocked transitions)
  - Mastery level changes are reflected (not just lock state changes)
- Add a `masteryChanged` flag to node data if the mastery level changed since last render (for potential future animation)
- The refreshTree function should be stable (useCallback) to prevent unnecessary re-renders

**4. End-to-end verification:**
- After a challenge awards XP and mastery advances from novice to apprentice:
  - Node border changes from faint blue glow to solid blue glow
  - If advancing from journeyman to expert, pulse animation starts
  - If advancing to expert/master, ember particles appear
  - If the advancement meets a downstream node's prerequisite, that node unlocks with animation
  </action>
  <verify>
    <automated>grep -q "data-mastery" components/tree/HexagonNode.tsx && grep -q "ember-rise" components/tree/tree-styles.css && grep -q "refreshTree\|refetchTree" components/tree/SkillTreeFlow.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - HexagonNode applies correct mastery-{level} CSS class for all 6 levels
    - Expert and master nodes have pulse animation (3s cycle)
    - Expert and master nodes have ember particle effects via ::after
    - SkillTreeFlow refreshTree correctly updates all node visuals
    - Build passes
  </acceptance_criteria>
  <done>Mastery visual progression fully connected: level changes update node glow/color, expert/master show particles, unlock animations fire when prerequisites met.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. All 6 mastery levels have distinct visual treatment
3. Expert/master nodes show pulse and ember animations
4. Tree refreshes correctly after challenge completion
</verification>

<success_criteria>
- Each mastery level has visually distinct node appearance (tested via CSS class application)
- Expert nodes pulse with 3s cycle in gold
- Master nodes pulse with 3s cycle in white-gold with more intense glow
- Expert/master nodes show ember particles rising above the node
- After challenge completion, node visual updates without page reload
- Mastery level-up correctly computed (XP threshold crossed → advance level)
</success_criteria>

<output>
After completion, create `.planning/phases/03-engagement-loop/03-03-SUMMARY.md`
</output>
